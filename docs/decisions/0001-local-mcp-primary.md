# 0001: stdio MCP is the primary interface

Mailhub is installed and launched locally as a stdio MCP server. REST and CLI remain local companion interfaces. The MCP server does not listen on the network and may work when the REST daemon is stopped.

Read-only mode is the default. Full mode is an explicit local-user choice.
